apiVersion: extensions/v1beta1
kind: DaemonSet
metadata:
  name: scan-data
spec:
  selector:
    matchLabels:
      name: scan-data
  template:
    metadata:
      labels:
        name: scan-data
    spec:
      nodeSelector:
        purpose: prod
      containers:
      - image: registry.centos.org/navidshaikh/ccp-openshift-scan
        imagePullPolicy: Always
        name: scan-data
      serviceAccount: jenkins
